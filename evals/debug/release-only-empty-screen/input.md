Our settings screen is blank in the Play Store build. Works perfectly in debug, works in the
release build on the iOS simulator, works on an Android debug build on a device.

No crash — the app doesn't close. The header renders, the list area is just empty. Nothing in
Crashlytics. We've tried clearing the app data and reinstalling.

RN 0.87, Hermes, New Architecture on.
