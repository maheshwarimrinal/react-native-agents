We're a 4-person team with one React Native app. Someone suggested moving to a monorepo so we can
later add a web app and share the API types.

Nothing is shared yet — the web app doesn't exist. Should we do it now?
