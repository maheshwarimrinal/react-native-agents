We've taken over a client's React Native app. Previous agency is gone, no handover, no docs.
Where do we start?

Here's what `ls` gives us at the root:

android/  ios/  src/  patches/  App.tsx  index.js  package.json  metro.config.js
babel.config.js  tsconfig.json  app.json  README.md  yarn.lock  package-lock.json
